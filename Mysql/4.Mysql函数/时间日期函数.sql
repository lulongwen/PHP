/** 时间日期函数
  没有表的情况下，用 dual
  select current_date() from dual;

  current_date()
  current_time()
  current_timestamp()

  date_add()
  date_sub()
  datediff()

  now()

  date(datetime) 给予时分秒会自动舍去
  year(datetime)
  month(datetime)

  from_unixtime()
  unix_timestamp()

 */