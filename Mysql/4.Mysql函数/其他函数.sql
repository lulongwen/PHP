/** 其他函数

  user()

  -- 查询用户 哑元表
  select user() from dual;

  datebase()
  -- 数据库名称

  MD5(str)
    md5 对字符串进行加密后得到一个 32字节的值
  select md5('abc') from dual;

  password(str) 得到一个加密后的字符串
  select * from mysql.user



 */

